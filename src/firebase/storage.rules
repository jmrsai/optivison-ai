rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Rules match for files in your storage bucket
    // By default, allow read/write access only to authenticated users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
